require('./bootstrap');
//map value from .env file to front-end

/*
window.MixConfig = {
    authClient: {
        client_id: process.env.MIX_PUBLIC_CLIENT_ID,
        client_secret: process.env.MIX_PUBLIC_CLIENT_SECRET,
    },
    pusher: {
        appKey: process.env.MIX_PUSHER_APP_KEY,
        appCluster: process.env.MIX_PUSHER_APP_CLUSTER
    },
    app: {
        name: process.env.MIX_APP_NAME,
        debug: process.env.MIX_APP_DEBUG,
        url: process.env.MIX_APP_URL,
        domain: process.env.MIX_APP_URL.replace('https://', '').replace('http://', '')
    }
}
*/
